from setuptools import setup, find_packages

setup(
    name="cluster_selector",
    packages=find_packages(),
    description="Provides an automated way to update the EKS hosted Kubernetes cluster you are connected to.",
    version="0.1.0",
    author="Jason Grant"
)
